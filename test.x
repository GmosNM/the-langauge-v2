

let fuck: int = 69;

fn add(x: int |  y: int): int{
    return x + y;
}

fn main(): int{
    let x: int = 10;
    let y: int = x / x;
    let z: int = y * 0;
    let w: int = y - z;
    let v: int = w + w;
    v = 100 + 100;
    return v;
}
